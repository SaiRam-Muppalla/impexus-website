# og-image.png

Place a 1200 × 630 px image here as `public/og-image.png`.

Referenced by:
- `<meta property="og:image">` in `index.html`
- `<meta name="twitter:image">` in `index.html`
- `topic.ogImage` fallback in `src/pages/TopicPage.tsx`

Recommended content: Impexus logo on a dark navy background with the tagline
"Empowering Future Tech Leaders — Campus Learning & Development".

Minimum size: 1200 × 630 px at 72 dpi, PNG or JPEG, under 1 MB.
